{
  "name": "devlist",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-js": "watchify client/main.js -o client/bundle.js -dv",
    "build-js": "browserify client/main.js | uglifyjs -mc > client/bundle.js",
    "watch": "npm run watch-js",
    "build": "npm run build-js",
    "prestart": "npm install && npm run build",
    "start": "node server.js"
  },
  "private": true,
  "dependencies": {
    "browserify": "^5.9.1",
    "express": "^4.7.2",
    "redis": "^0.11.0",
    "uglify-js": "^2.4.15"
  },
  "devDependencies": {
    "watchify": "^1.0.1"
  }
}
